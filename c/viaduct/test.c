
//#include "tao.h"
#include <stdio.h>
#include <stdlib.h>
#include <config.h>
#include <headers.h>

int main( int argc, char *argv[]) {
				char **tmp;
				tao_opt *plaap;

				plaap->src.file = "plaap";
				printf("plaap %s\n", plaap->src.file);
				exit(0);
}
//
//				if( strtol( argv[argc -1], tmp, 10) ) {
//								printf("string\n");
//				}
				
